# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

node_modules
dist
.DS_Store
src-tauri/target
*.nosync
.env

node_modules
dist
.DS_Store
src-tauri/target
*.nosync
.env

# macOS signing credentials — never commit these
.env.signing

# Machine-specific cargo target-dir override (do not commit)
src-tauri/.cargo/config.toml

# Swift sidecars built by build.rs; a non-macOS host only ever writes stubs here.
src-tauri/binaries/video-scan

src-tauri/binaries/frame-extract
